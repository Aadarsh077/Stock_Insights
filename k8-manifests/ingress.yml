apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stock-app-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: stock-insights.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: stock-app-service
            port:
              number: 80
